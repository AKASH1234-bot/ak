services:
  - type: web
    name: ak
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app & python3 bot.py
    envVars:
      - key: PORT
        value: 8080
